# Stub directory

Canonical content is at `../templates` in the repository root.

This stub replaces a symlink so GitHub Actions ZIP extraction (which does not preserve symlinks) satisfies runner file validation.
